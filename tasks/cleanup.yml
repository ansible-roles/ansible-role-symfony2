---

- name: Symfony2 | Cleanup | Remove old releases
  shell: >
    ls -dr {{ symfony2_root }}/releases/* | egrep '{{ symfony2_release_regexp }}' | tail -n +{{ symfony2_releases + 1 }} | xargs rm -frv
  when: symfony2_releases > 0
